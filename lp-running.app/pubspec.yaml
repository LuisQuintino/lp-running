name: lp_runningflutter
description: "lp_runningflutter."

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.19.0
  mask_text_input_formatter: ^2.9.0
  image_picker: ^1.1.2
  flutter_multi_formatter: ^2.1.0
  fluttertoast: ^8.0.9
  csv: ^6.0.0
  path_provider: ^2.0.11
  pdf: ^3.8.0
  excel: ^2.0.6
  flutter_file_saver: ^0.7.0+1
  universal_html: ^2.0.6 # Substituído 'html' por 'universal_html' para maior compatibilidade
  printing: ^5.10.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
